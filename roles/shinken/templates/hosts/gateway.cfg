define host{
        use                     generic-host
	contact_groups		admins
        host_name               {{ hosts.gateway.hostname }}
        address                 {{ hosts.gateway.hostname }}.{{ domain.name }}.{{ domain.extension }}
        }

